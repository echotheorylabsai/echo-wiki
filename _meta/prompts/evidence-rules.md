# Evidence Rules

Place one standalone `Evidence: raw/<path>.md#<exact heading>` line immediately after every factual paragraph. Use multiple consecutive evidence lines when a paragraph depends on more than one source.

Do not cite external knowledge. Mark unsourced reasoning as `Inference:` and unresolved information as `Open question:`. The locator must use a raw Markdown file and a heading whose visible text matches exactly.
