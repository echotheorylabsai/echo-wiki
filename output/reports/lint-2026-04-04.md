# Wiki Lint Report — 2026-04-04

**Scope:** all
**Articles checked:** 14
**Issues found:** 2 critical, 4 warnings, 5 suggestions

## Critical Issues

### 1. Orphaned source summaries — zero inbound wikilinks
The following articles have no inbound links from any compiled article body (per `_backlinks.md`):

- `compiled/sources/demystifying-evals-for-ai-agents.md` — not linked from any article body
- `compiled/sources/how-to-build-custom-agent-framework-pi.md` — not linked from any article body

**Impact:** These articles are unreachable through wiki navigation. They appear in `_index.md` but no article's body text links to them.

**Fix:** Add body-text wikilinks from relevant concept or tool articles. For example, `concepts/agent-evaluation` should link to `[[sources/demystifying-evals-for-ai-agents]]` and `concepts/agent-harness` or `tools/pi-framework` should link to `[[sources/how-to-build-custom-agent-framework-pi]]`.

### 2. Invalid `source_url` on source-summary article
`compiled/sources/agent-harness-engineering-guide.md` has `source_url: "local"`. The frontmatter schema requires `source_url: url` for source-summary type. The string `"local"` is not a valid URL.

**Fix:** Replace with the actual URL or a file path URI (e.g., `file://...`). If truly local-only, consider adding a `local` sentinel to the schema's allowed values.

## Warnings

### 3. Low inbound link count on source summaries
These source summaries have only 1 inbound link each (from `concepts/harness-engineering` only):

- `compiled/sources/agent-harness-engineering-guide.md` — 1 inbound link
- `compiled/sources/awesome-harness-engineering.md` — 1 inbound link

**Impact:** Source summaries are underlinked. Multiple articles reference these sources in their frontmatter `sources` field but don't link to the corresponding source-summary article in their body text.

### 4. `author: "Unknown"` on source-summary
`compiled/sources/agent-harness-engineering-guide.md` has `author: "Unknown"`. While technically valid (it's a string), this indicates incomplete metadata.

**Fix:** Identify and fill in the actual author from the raw source.

### 5. `follows` field may contain outdated URL
`compiled/people/nader-dabit.md` has `follows: ["https://nader.substack.com", "https://twitter.com/daaboross"]`. The Twitter handle `@daaboross` does not appear to match Nader Dabit — this may be incorrect.

**Fix:** Verify the correct Twitter/X handle for Nader Dabit.

### 6. No `mcp` or `fine-tuning` domain tags in use
The wiki config defines 6 domains: `llm`, `agents`, `harness-engineering`, `evals`, `mcp`, `fine-tuning`. Currently:
- `mcp` — 0 articles tagged
- `fine-tuning` — 0 articles tagged
- `llm` — 1 article tagged (`concepts/context-engineering`)

**Impact:** Two entire domains have zero coverage. This is expected for a young wiki but worth tracking.

## Suggestions

### 7. Missing concept candidate: "Anthropic"
Mentioned by name in 6+ articles (`concepts/harness-engineering`, `concepts/agent-evaluation`, `concepts/context-engineering`, `sources/demystifying-evals-for-ai-agents`, `sources/awesome-harness-engineering`, `tools/harbor-eval`). Could warrant a `people/` or `tools/` article, or at minimum a concept entry on their agent infrastructure contributions.

### 8. Missing concept candidate: "LangChain"
Mentioned in 3 articles (`concepts/harness-engineering`, `concepts/agent-harness`, `sources/awesome-harness-engineering`). Relevant as both a tool and a conceptual reference point for the harness-vs-framework distinction.

### 9. Missing concept candidate: "permission gates" / "safe autonomy"
Referenced across 4+ articles (`concepts/agent-harness`, `concepts/harness-engineering`, `sources/agent-harness-engineering-guide`, `sources/awesome-harness-engineering`). This is a distinct harness engineering concern that could be its own concept article.

### 10. Missing concept candidate: "observability"
Mentioned in 3+ articles (`concepts/harness-engineering`, `sources/awesome-harness-engineering`, `sources/demystifying-evals-for-ai-agents`). A core pillar of harness engineering with no dedicated article.

### 11. No duplicate articles detected
All 14 articles cover distinct entities. No naming conflicts or overlapping subjects found.

---

### Check Summary

| Check | Status | Details |
|---|---|---|
| 1. Frontmatter validation | 1 issue | `source_url: "local"` is not a valid URL |
| 2. Broken wikilinks | Pass | All 4 raw links and 14 compiled links resolve correctly |
| 3. Orphaned articles | 2 found | 2 source summaries have zero inbound body links |
| 4. Contradictory claims | Pass | No factual contradictions detected across article pairs |
| 5. Stale content | Pass | All articles verified today (2026-04-04); none stale |
| 6. Missing concept candidates | 4 found | Anthropic, LangChain, permission gates, observability |
| 7. Duplicate detection | Pass | No duplicates found |

---
Generated by /lint skill
